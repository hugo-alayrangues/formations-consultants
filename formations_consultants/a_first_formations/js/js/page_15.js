const headerElt = document.querySelector("header");

headerElt.insertAdjacentHTML("beforebegin", "<p class='blue'>beforebegin</p>");

headerElt.insertAdjacentHTML("afterbegin", "<p class='blue'>afterbegin</p>");

headerElt.insertAdjacentHTML("beforeend", "<p class='blue'>beforeend</p>");

headerElt.insertAdjacentHTML("afterend", "<p class='blue'>afterend</p>");

// Attention aux guillemets !






